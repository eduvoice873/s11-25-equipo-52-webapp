export const metadata = {
  title: 'Testimonios Widget',
  description: 'Widget embebible de testimonios',
};

export default function EmbedLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
